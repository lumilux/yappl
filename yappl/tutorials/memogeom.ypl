fun (fun int int):geom_list_gen float:p =
	fun int:geom_list int:n := ~geom p
	in geom_list
in
~seed;
let (fun int int):g = ~geom_list_gen 0.5 in
~print_line [1, ~g 1, ~g 1];
~print_line [2, ~g 2, ~g 2];
~print_line [3, ~g 3, ~g 3];
~print_line [4, ~g 4, ~g 4];
