let int:a = 4 and int:b = 5 in ~print_line ( a + b );
let int[]:x = [1,2,3] in ~print_line x[2] + x[0];
let fun float:r = rand in ~rand;
fun (fun int int):f int:x = 
    fun int:g int:y = 
    	x + y 
    in 
    g 
in
let (fun int int):h = ~f 4 in
~print_line ~h 2